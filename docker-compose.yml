version: '3'

services:
  nginx:
    build: ./nginx
    ports:
      - "9000:80"
    depends_on:
      - backend
      - organizer

  backend:
    build: ./backend

  organizer:
    build: ./organizer
